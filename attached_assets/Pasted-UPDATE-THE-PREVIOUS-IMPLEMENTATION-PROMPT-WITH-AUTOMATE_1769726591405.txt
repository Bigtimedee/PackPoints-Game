UPDATE THE PREVIOUS IMPLEMENTATION PROMPT WITH AUTOMATED TESTING ENABLED

You are modifying PackPTS’ card image ingestion and match delivery pipeline. After implementation, you MUST execute automated validation tests and log the results. Do NOT rely on manual browser testing.

========================================================
GOAL

Guarantee that every PackPTS match always contains 10 playable cards with valid images by:

- Validating images server-side
- Proxying all card images through PackPTS
- Blocking placeholder / broken / hotlinked images
- Automatically replacing failed cards
- Running post-deploy automated verification

========================================================
A) DATABASE (UNCHANGED)

Implement these tables if not present:

card_image_cache
- card_id TEXT PRIMARY KEY
- source_url TEXT
- normalized_url TEXT
- proxied_path TEXT
- status TEXT            // ok | bad | pending
- last_http_status INT
- last_content_type TEXT
- bytes INT
- fail_count INT DEFAULT 0
- last_checked_at TIMESTAMP DEFAULT now()

card_image_quarantine
- card_id TEXT PRIMARY KEY
- reason TEXT
- seen_count INT DEFAULT 1
- last_seen_at TIMESTAMP DEFAULT now()

========================================================
B) IMAGE NORMALIZATION + VALIDATION SERVICE

Create src/services/images/imageGate.ts

Implement:

normalizeImageUrl(url)
- Convert "//" → "https://"
- Prefer https over http
- Trim spaces

validateRemoteImage(url)
- Timeout: 6s
- Follow redirects
- Use Range bytes=0-65535
- Accept only:
  status 200 or 206
  content-type startsWith "image/"
  bytes between 5 KB and 10 MB

Reject:
- example / placeholder / appforest / noimage in URL
- content-type not image
- tiny images

========================================================
C) IMAGE PROXY ENDPOINT

Add:

GET /api/images/card/:cardId

Behavior:
- Look up card_id in card_image_cache
- Only allow status='ok'
- Stream remote image through server
- Set headers:
  Content-Type
  Cache-Control: public, max-age=86400
- Return 404 if invalid or missing

========================================================
D) MATCH BUILD PIPELINE (HARD REQUIREMENT)

Modify match creation:

1) Pull batch of cards from Card Hedge
2) For each card:
   - normalize URL
   - skip quarantined
   - validateRemoteImage
   - cache results
3) ONLY accept cards where status='ok'
4) Continue fetching until 10 valid cards collected
5) If not enough cards after retries:
   Abort match creation with error:
   NO_PLAYABLE_CARDS_AVAILABLE

Save:
match_questions.image_url = proxied_path ONLY

========================================================
E) FRONTEND IMAGE LOADING

Replace ALL card image usage:

<img src={`/api/images/card/${cardId}`} />

On error:
Show:
"Unable to load card image"
Button:
"Try another card"

========================================================
F) CARD RESYNC API

Add:

POST /api/matches/:id/resync-card
Body:
{ idx: number }

Server logic:
- Validate match active
- Validate requester is participant
- Replace ONLY that question index
- Select new validated card not already used
- Broadcast websocket event:
MATCH_QUESTION_REPLACED

========================================================
G) AUTOMATED TESTS (REQUIRED)

After implementation create test suite:

File: tests/card-image-pipeline.test.ts

Tests must include:

1) Image Validation Test
- Feed known bad placeholder URL
- Expect status='bad'

2) Proxy Endpoint Test
- Request proxied image
- Expect:
  HTTP 200
  content-type image/*
  non-zero body length

3) Match Build Test
- Create match
- Assert:
  exactly 10 questions
  all image_url fields start with "/api/images/card/"
  none reference external domains

4) Resync Card Test
- Trigger resync-card
- Assert:
  question replaced
  index preserved
  new card_id differs

5) Failure Safety Test
- Force invalid card pool
- Expect:
  match creation fails cleanly with NO_PLAYABLE_CARDS_AVAILABLE

========================================================
H) POST-DEPLOY VERIFICATION SCRIPT

Add npm script:

"verify:images": 
- Build 5 sample matches
- Log:
  match_id
  count of playable images
  failed validations

Script must exit non-zero if any match has <10 playable images.

========================================================
I) FINAL REQUIREMENT

After coding:
- Run test suite
- Run verification script
- Log output to console

Do NOT skip test execution.

Proceed with implementation now.