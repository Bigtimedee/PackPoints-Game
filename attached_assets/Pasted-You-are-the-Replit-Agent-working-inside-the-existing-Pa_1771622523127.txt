You are the Replit Agent working inside the existing PackPTS repo.

MISSION
Implement an automated “Video Factory” that generates actual vertical MP4 videos for TikTok/Reels/Shorts from PackPTS card content — not just scripts — using template-driven rendering (FFmpeg + optional TTS). The system must:
- Produce 9:16 MP4 videos from a card image + metadata (hook, countdown, reveal, CTA).
- Apply PackPTS masking rules (never show player name unmasked).
- Export a video file + thumbnail + metadata and attach them to the existing Publishing Queue (manual posting).
- Be safe, deterministic, and debuggable.

CONSTRAINTS
- Do NOT rely on TikTok APIs. This is asset generation only.
- Use FFmpeg (available on Replit) as primary renderer.
- Use OpenAI TTS optionally; if no OPENAI_API_KEY or TTS disabled, generate videos without voice.
- Must never output “silhouette” or placeholder images; validate inputs and retry selection.
- Must not break existing gameplay. Add as an isolated module + admin tools.

========================================================
PART 1 — INSTALL/VERIFY DEPENDENCIES
========================================================
1) Verify ffmpeg exists in the environment:
- Add a startup check to log `ffmpeg -version` once in dev.
- If missing, add it via Replit system packages or document install steps.

2) Add Node deps:
- fluent-ffmpeg (or spawn ffmpeg directly; prefer spawn for reliability)
- zod for validation (already in repo)
- sharp for image pre-processing (optional but recommended)

========================================================
PART 2 — VIDEO TEMPLATE SYSTEM (5 templates)
========================================================
Create: server/videoFactory/templates.ts

Define a small template spec:
- template_id: "classic_countdown" | "difficulty_ladder" | "only_real_fans" (start with 1–2 templates)
- duration_sec (e.g., 12–18)
- scenes array:
  - scene_id
  - start_sec
  - end_sec
  - layers: [ {type: "image"|"text"|"timer"|"sfx", ...} ]

Implement at least ONE template end-to-end:
TEMPLATE: "classic_countdown" (12s)
Scene 1 (0–2s): blurred/masked card + hook text overlay
Scene 2 (2–6s): countdown 3-2-1 + “Who is it?” overlay
Scene 3 (6–10s): reveal (still keep name masked on the card itself; reveal via separate text overlay) + “Answer: ____”
Scene 4 (10–12s): CTA overlay: “Play PackPTS.com • Daily 5 • Beat my score”

IMPORTANT MASKING RULE
- Never show the player name on the card image. Mask it in the card image layer.
- The “reveal” is allowed only as overlay text, not on the card itself.

========================================================
PART 3 — INPUT VALIDATION & IMAGE SANITY CHECKS
========================================================
Create: server/videoFactory/validate.ts

Implement:
- reject placeholder/silhouette images:
  - if URL contains "silhouette", "placeholder", "default", "example.jpg" etc.
  - AND/OR HEAD request content-length too small (< 8KB)
  - AND/OR image hash matches known placeholder hashes (add later; start with URL + size checks)

Implement:
- downloadImageToTemp(url) with timeout, size limit (10MB)
- verify image decodes (sharp)
- ensure dimensions reasonable (min width 200)
- run card masking step:
  - Use existing masking logic if present
  - If not, implement a “safe mask” overlay strategy:
    - Add black/blur rectangles at typical name positions:
      - top band: y=0..18% height
      - bottom band: y=78%..100% height
    - (This prevents name leaks even without OCR.)
  - Output maskedCard.png

========================================================
PART 4 — FFmpeg RENDERER
========================================================
Create: server/videoFactory/render.ts

Implement renderVideoClassicCountdown(input):
Input object:
{
  cardImagePath,           // maskedCard.png
  hookText,                // short
  answerText,              // player name (overlay only)
  ctaText,                 // “Play PackPTS.com”
  outputDir,
  durationSec=12,
  includeVoice=false,
  voiceAudioPath?          // optional
}

Rendering requirements:
- Output 1080x1920 (or 720x1280 as fallback) MP4 H.264 AAC
- Background: subtle dark gradient or blurred card background generated from the card image
- Foreground: card centered, scaled to fit
- Text overlays:
  - Hook (0–2s)
  - Countdown (2–6s) (3,2,1 changing each second)
  - Reveal overlay (6–10s) "Answer: {answerText}"
  - CTA (10–12s)
- Audio:
  - If voice provided, mix with low-volume tick/beep SFX (optional)
  - If no voice, silent output is acceptable

Implementation approach:
- Prefer spawning ffmpeg with a filter_complex graph:
  - scale/pad to 1080x1920
  - drawtext for overlays with enable='between(t,...)'
  - use a generated color source for background + overlay card
- Keep font path generic; use a bundled font in repo:
  - Add /assets/fonts/Inter-Bold.ttf
  - Reference it in drawtext

Also generate a thumbnail:
- Extract frame at t=1.0s to output thumbnail.jpg

========================================================
PART 5 — OPTIONAL OPENAI TTS VOICEOVER
========================================================
Create: server/videoFactory/tts.ts

If OPENAI_API_KEY exists and VIDEO_TTS_ENABLED=true:
- Generate a short voiceover audio (mp3 or wav) for:
  - hook + countdown prompt + reveal + CTA
Example text:
"{hookText}. Three… two… one… Answer: {answerText}. Play PackPTS dot com."
- Save as voice.mp3
- Convert to wav if needed for ffmpeg

Env vars:
- VIDEO_FACTORY_ENABLED=true
- VIDEO_TTS_ENABLED=true/false
- OPENAI_API_KEY=...

========================================================
PART 6 — INTEGRATE WITH PUBLISHING QUEUE
========================================================
When a TikTok publishing package is generated by Growth Agent:
- Create (or update) the content item metadata to include:
  - video_asset: { path or url, thumbnail_path }
  - rendering: { template_id, duration, created_at }
  - status stays READY

Create a service:
server/videoFactory/index.ts
- generateVideoForContentItem(contentItemId)
  - loads TikTok package from DB
  - selects card image URL from asset_refs (or card_id -> image url)
  - validates/masks image
  - renders video + thumbnail
  - updates DB with file references
  - marks content item READY (or FAILED with error)

Store outputs in:
- /public/generated/videos/{date}/{contentItemId}.mp4
- /public/generated/videos/{date}/{contentItemId}.jpg
(or in an S3 bucket if configured; start local)

========================================================
PART 7 — ADMIN UI: “RENDER VIDEO” + DOWNLOAD
========================================================
Extend Admin → Growth → Publishing Queue:

For TikTok items:
- Show “Render Video” button if video not generated
- Show “Re-render” button (admin only) with confirmation
- Show “Download MP4” and “Download Thumbnail”
- Show “Copy Caption + Hashtags”
- Show “Mark as Posted”

Add endpoint:
POST /api/admin/growth/queue/:id/render-video
- Admin only
- triggers generation
- returns updated item

Also add “bulk render” for up to 5 items at a time with throttling.

========================================================
PART 8 — BACKGROUND WORKER (OPTIONAL BUT RECOMMENDED)
========================================================
If Growth Agent worker exists:
- Add a job: renderTikTokVideos
  - Runs after generateTikTokPackages
  - Renders videos for the day’s READY TikTok items (max N per hour)
  - Uses a concurrency limit (1–2) to avoid CPU overload on Replit

========================================================
PART 9 — SAFETY / ACCEPTANCE TESTS (YOU RUN THEM)
========================================================
After implementation, run these tests in the repl:

1) Unit: validate rejects placeholder image URLs
2) Integration: render one video from a known-good card image and verify:
   - mp4 exists
   - thumbnail exists
   - duration ~12s
   - video dimensions 1080x1920 (or 720x1280)
3) Masking: confirm name never appears on card image layer by always applying top/bottom masks.
4) Admin flow: click “Render Video”, then “Download MP4”.

IMPORTANT: Do NOT attempt to auto-post to TikTok.

DELIVER BACK TO ME
- List files created/changed
- Exact env vars needed
- Where the generated MP4s are stored
- A short “How to use” note: Admin steps to render and download videos