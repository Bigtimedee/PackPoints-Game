You are the Replit Agent working inside the existing PackPTS repo.

OBJECTIVE
Implement “Viral TikTok Formats for Sports Cards” as first-class, reusable VIDEO TEMPLATES + CONTENT GENERATORS inside the PackPTS Growth Agent + Video Factory. The system must allow an Admin to select a format and automatically generate:
- A strict TikTok package (hook, script, caption, hashtags, overlays, shot list)
- A rendered MP4 (via the existing Video Factory)
- A Publishing Queue item ready for manual posting

SCOPE
Implement 6 proven formats (templates) tailored to sports cards:
A) Only Real Fans
B) Difficulty Ladder (Easy/Medium/Impossible)
C) Memory Shock (“Remember this guy?”)
D) Pack Pull Drama (digital pack opening cadence)
E) Leaderboard Flex (real users, scores, streaks)
F) Era Wars (90s vs 2000s vs today)

NON-NEGOTIABLES
- Must remain MANUAL posting (no TikTok auto-post).
- Must NEVER show unmasked player name on the card image; reveals only via overlay text.
- Must avoid silhouette/placeholder images; validate and retry card selection.
- Must be deterministic, deduped, and safe (no runaway generation).
- Keep everything in Admin → Growth → Publishing Queue with previews and “Render Video” buttons.

======================================================================
PART 1 — DATA MODEL UPDATES
======================================================================
1) Extend TikTok metadata schema to include:
- format_id: one of ["only_real_fans","difficulty_ladder","memory_shock","pack_pull_drama","leaderboard_flex","era_wars"]
- scenes: array (optional) describing overlay text per scene
- render_template_id: string mapping to video factory templates
- cards: array of selected card refs with difficulty tags (if relevant)
- engagement_goal: "comments" | "shares" | "replays" | "conversion"
- safety_flags: { no_gambling_language: true, no_prize_guarantees: true }

2) Add a lookup table of formats:
- growth_formats
  - id (format_id)
  - name
  - description
  - default_duration_sec
  - default_scene_timing (json)
  - recommended_posting_window (json)
  - created_at

Seed the 6 formats into this table via migration or seed script.

======================================================================
PART 2 — VIDEO FACTORY TEMPLATES (RENDER LAYER)
======================================================================
Implement 6 corresponding templates in server/videoFactory/templates.ts.
Each template outputs a vertical MP4 with deterministic scene timing and overlays.

A) only_real_fans (12s)
- 0–2s: “ONLY REAL FANS GET THIS” + blurred card
- 2–6s: countdown “3…2…1…” + “WHO IS IT?”
- 6–10s: “Answer:” overlay text reveal + keep card masked
- 10–12s: CTA “Play PackPTS.com • Daily 5”

B) difficulty_ladder (15s)
- 0–3s Easy card (masked) + “EASY”
- 3–6s reveal overlay
- 6–9s Medium card + “MEDIUM”
- 9–12s reveal overlay
- 12–15s Impossible card + “IMPOSSIBLE” + CTA
Requirements:
- select 3 cards tagged easy/medium/hard by a heuristic (see Part 3).

C) memory_shock (12s)
- 0–3s “REMEMBER THIS GUY?” + masked card
- 3–7s “Where did he play?” prompt + 3 options overlay
- 7–10s reveal overlay
- 10–12s CTA + “Streaks are live”

D) pack_pull_drama (15s)
- 0–5s simulated “pack opening” animation using fast cuts of card backs / blurred fronts
- 5–12s “big reveal” (still masked on card) + overlay answer
- 12–15s CTA
If no card-back assets exist, implement with motion/zoom/cut effects on the same image.

E) leaderboard_flex (12s)
- 0–3s show “DAILY 5 TOP PLAYERS” overlay
- 3–9s show 2–3 winners (username + score + streak) overlays; optionally show their challenge card image masked
- 9–12s CTA “Can you beat them tonight? PackPTS.com”

F) era_wars (12s)
- 0–3s “ERA WARS: 90s vs 2000s” overlay
- 3–9s show two masked cards from different eras + “Which era wins?” overlay
- 9–12s CTA + “Comment your era”

Implement templates using your existing FFmpeg render pipeline (drawtext + timing + image layers). Reuse shared helpers.

======================================================================
PART 3 — CARD SELECTION HEURISTICS (CONTENT LAYER)
======================================================================
Create server/growthAgent/cardSelector.ts with safe selection and optional difficulty tagging.

Global constraints:
- Must be from playable sets already loaded
- Must pass “no silhouette / no placeholder” checks
- Must have usable image URL
- Must NOT be removed by any pruning process
- Use deterministic seeded RNG per (date + format_id + set_id + SECRET_SALT)

Heuristics:
1) “difficulty” score:
- Famous players -> fewer points / easier:
  - Use 30-day sales volume proxy if available, or keyword list (“rookie”, star names list) if not.
- Obscure players -> harder:
  - low sales volume OR missing sales data OR lower popularity.
Return: easy/medium/hard buckets.

2) Era detection:
- Parse set year from set name (e.g., “1987 Topps Baseball” -> 1987)
- Map to era: 70s/80s/90s/00s/10s/20s

3) Memory shock:
- Prefer “name recognition” but not top 5 stars (mid-tier nostalgia).

======================================================================
PART 4 — PROMPT PACKS (STRICT JSON GENERATORS)
======================================================================
Add prompt templates and zod schemas:

- prompts/generator.tiktok_only_real_fans.txt
- prompts/generator.tiktok_difficulty_ladder.txt
- prompts/generator.tiktok_memory_shock.txt
- prompts/generator.tiktok_pack_pull_drama.txt
- prompts/generator.tiktok_leaderboard_flex.txt
- prompts/generator.tiktok_era_wars.txt

Each generator must output STRICT JSON matching TikTokPackageSchema plus format_id and scenes.

Also add:
- prompts/validator.tiktok_compliance.txt
Rules:
- no gambling language
- no guaranteed prizes
- no misleading claims
- always CTA
- keep captions under 200 chars preferred

Implement orchestrator logic:
- generate package
- validate compliance
- auto-rewrite once if flagged
- store final

======================================================================
PART 5 — GROWTH AGENT JOBS
======================================================================
Add a job: generateViralTikTokQueue
- Runs daily and creates 3–6 TikTok items/day, mixing formats:
  - 2x Only Real Fans
  - 1x Difficulty Ladder
  - 1x Memory Shock
  - 1x Era Wars
  - (and optionally Pack Pull Drama / Leaderboard Flex depending on data availability)
- Create content items with platform="tiktok", posting_mode="MANUAL_QUEUE", status="READY"
- Dedup by (content_date + format_id + render_template_id + set_id)

Add a job: renderTikTokQueueVideos
- Runs after generation
- Renders MP4s for READY items missing video_asset
- Concurrency limit 1–2
- Marks FAILED with error if rendering fails; do not loop endlessly

======================================================================
PART 6 — ADMIN UI
======================================================================
In Admin → Growth → Publishing Queue:
1) Add “Format” filter dropdown (6 formats)
2) Show format badge on each item
3) Provide preview components:
- Hook
- Scene overlays list
- Caption + hashtags
- Selected cards list (count, set year)
- Video status (Not rendered / Rendered)

4) Add action buttons:
- Render Video / Re-render
- Download MP4 / Thumbnail
- Copy caption / hashtags / script
- Mark posted

5) Add “Create One Now” admin tool:
- Admin selects:
  - Format
  - Sport category
  - Set (optional)
  - Scheduled_for (optional)
- Click Generate
- It creates a single item + optionally renders immediately
This helps you iterate on what performs.

======================================================================
PART 7 — ACCEPTANCE TESTS (RUN AFTER IMPLEMENTATION)
======================================================================
1) Generate one item per format and ensure JSON validates.
2) Render one video per format and confirm:
- mp4 exists, vertical, correct duration
- name masked on card image
3) Verify no duplicates on re-run.
4) Admin can copy caption/hashtags and mark posted.

DELIVERABLES
- Code changes implementing all 6 viral formats end-to-end
- Updated docs explaining formats and how Admin generates + downloads
- Clear list of env vars
- A brief “How to use in production” note

IMPORTANT: Do not attempt to auto-post to TikTok. Manual queue only.