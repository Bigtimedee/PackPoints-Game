You are the Replit Agent working inside the existing PackPTS repo.

OBJECTIVE
Implement TikTok “manual mode” integration for the PackPTS Growth Agent using a Publishing Queue workflow:
- Step 2: Add TikTok as an enabled platform with MANUAL posting mode via env vars.
- Step 3: Ensure DB schema + types support TikTok publishing packages (script, caption, hashtags, hooks, overlays, asset refs, etc.).
- Step 4: Add Admin UI actions: “Copy TikTok Caption”, “Copy Hashtags”, “Download Script”, “Download Asset Pack (zip optional)”, and “Mark as Posted”.

NON-NEGOTIABLES
- Do NOT attempt to auto-post to TikTok (no TikTok API posting). This is MANUAL mode only.
- Do NOT break existing Growth Agent or Admin portal functionality.
- All TikTok items must be generated/stored by the Growth Agent and surfaced in Admin → Growth → Publishing Queue.
- Idempotent: TikTok queue items should not duplicate for the same plan/date/content type.
- Admin-only access; reuse existing Admin auth/RBAC.

──────────────────────────────────────────────────────────────────────────────
A) ENV VARS / CONFIG (Step 2)
──────────────────────────────────────────────────────────────────────────────
1) Add Growth Agent platform config with TikTok support.

Implement a config module (or extend existing):
- server/growthAgent/config.ts

Read these env vars:
- GROWTH_TIKTOK_ENABLED (boolean, default false)
- GROWTH_TIKTOK_MODE ("manual" | "off", default "manual" when enabled)
- GROWTH_DEFAULT_PLATFORMS (comma list, optional)

Rules:
- If GROWTH_TIKTOK_ENABLED=true, TikTok should appear in the platform list with posting_mode="MANUAL_QUEUE".
- If disabled, Growth Agent never generates TikTok content items.

2) Add documentation:
- docs/GROWTH_AGENT.md: explain TikTok manual mode and required env vars.

3) Ensure Admin UI visibly shows whether TikTok is enabled and in manual mode.

──────────────────────────────────────────────────────────────────────────────
B) DB SCHEMA + TYPES (Step 3)
──────────────────────────────────────────────────────────────────────────────
We need a structured “Publishing Package” for TikTok. Implement this in DB so the agent can store it and the Admin can copy it.

1) If we already have growth_content_items and/or publishing_queue tables:
- Extend them to support TikTok packages.

Minimum fields required for TikTok content items:
- platform = "tiktok"
- type = one of:
  - "TIKTOK_DAILY5_ANNOUNCEMENT"
  - "TIKTOK_TRIVIA_CHALLENGE"
  - "TIKTOK_LEADERBOARD_SPOTLIGHT"
  - "TIKTOK_STREAK_REMINDER"
  (You may implement as strings if enums are difficult.)

- posting_mode = "MANUAL_QUEUE"
- status = "READY" | "POSTED" | "FAILED"
- scheduled_for (timestamp)
- posted_at (timestamp nullable)

2) metadata JSON schema for TikTok items (store under growth_content_items.metadata OR publishing_queue.assets):
Include:
{
  "hook": "string (1 line)",
  "script": "string (voiceover script, 15-35s)",
  "on_screen_text": ["string", "string", ...] (overlays),
  "caption": "string (<= 2200 chars; prefer <= 200)",
  "hashtags": ["#packpts", "#baseballcards", ...] (10-20),
  "cta": "string",
  "thumbnail_text": "string (<= 6 words)",
  "format_notes": "string (shot list / timing / countdown / reveal)",
  "audio_notes": "string (optional)",
  "asset_refs": [
    { "type": "card_image", "card_id": "string", "url": "string" },
    { "type": "screenshot", "path": "string" },
    { "type": "gif", "path": "string" }
  ],
  "legal_safe": {
    "no_gambling_language": true,
    "no_prize_guarantees": true
  },
  "dedupe_key": "string"
}

3) Add DB-level dedupe:
- Add a unique index on (platform, date, type, dedupe_key) if you have date on item; otherwise unique on (platform, type, scheduled_for::date, dedupe_key).
If you don’t have date, add `content_date` (YYYY-MM-DD) to growth_content_items and index that.

4) Create migrations using Drizzle for these changes, and run them.

5) Add TypeScript types + Zod validation:
- server/growthAgent/prompts/schemas.ts:
  - TikTokPackageSchema (zod) matching above metadata shape.
Validate before insert. If invalid, mark the job failed and log why.

──────────────────────────────────────────────────────────────────────────────
C) GROWTH AGENT GENERATION (TikTok queue items)
──────────────────────────────────────────────────────────────────────────────
1) Add a generator prompt template:
- server/growthAgent/prompts/generator.tiktok_package.txt
It must output STRICT JSON that matches TikTokPackageSchema.

2) Create a job:
- server/growthAgent/jobs/generateTikTokPackages.ts

Behavior:
- Runs daily after the daily plan is generated/approved.
- For the chosen focus (sport/set + Daily5 theme), generate 3–5 TikTok packages/day:
  - Daily5 announcement (scheduled_for = today @ 8:00pm ET)
  - Trivia challenge (scheduled_for morning)
  - Leaderboard spotlight (scheduled_for after Daily5 close)
- Each generated item saved as growth_content_items with platform="tiktok" and posting_mode="MANUAL_QUEUE" and status="READY".
- Use a deterministic dedupe_key (e.g., `${content_date}:${type}:${setId}:${seed}`) so reruns don’t create duplicates.

3) Make sure TikTok generation only runs when GROWTH_TIKTOK_ENABLED=true.

──────────────────────────────────────────────────────────────────────────────
D) ADMIN UI (Step 4)
──────────────────────────────────────────────────────────────────────────────
Implement Admin → Growth → Publishing Queue features for TikTok items.
If the Admin Growth UI already exists, extend it. Otherwise create:
- /admin/growth/publishing-queue

1) Filter controls:
- platform filter (default “tiktok”)
- status filter (READY/POSTED)
- date filter (today, yesterday, custom)

2) Each TikTok queue card must show:
- Hook
- Thumbnail text
- Caption preview
- Hashtags preview
- Scheduled time
- Status pill (READY/POSTED)

3) Add the following buttons for each item:
- “Copy Caption” → copies metadata.caption to clipboard
- “Copy Hashtags” → copies hashtags joined with spaces
- “Copy Caption + Hashtags” → caption + newline + hashtags
- “Copy Script” → metadata.script
- “Download Script (.txt)” → downloads a txt file containing hook + script + caption + hashtags + shot list
- “Download Asset List” → downloads a JSON file of asset_refs (we are not required to zip real images yet; keep simple and reliable)
- “Mark as Posted” → updates status=POSTED, posted_at=now, posted_by=current admin user
- “Undo Posted” (optional) → revert to READY if needed

4) Add a “Bulk actions” row:
- Select multiple READY items
- Bulk “Mark as Posted”
- Bulk “Copy Captions” (concatenate)

5) Add a “Manual Posting Checklist” panel:
- Open TikTok app
- Create post
- Paste caption + hashtags
- Record posted time
- Mark as posted in PackPTS
(Just UI text.)

6) Ensure admin-only protection:
- If user not Admin, return 403 / redirect.

──────────────────────────────────────────────────────────────────────────────
E) API ENDPOINTS (for Admin UI)
──────────────────────────────────────────────────────────────────────────────
Add minimal APIs:
1) GET /api/admin/growth/queue?platform=tiktok&status=READY&date=YYYY-MM-DD
Returns list of content items (id, scheduled_for, status, metadata subset).

2) POST /api/admin/growth/queue/:id/mark-posted
Body: { posted: true }
Sets status to POSTED, posted_at, posted_by.
Idempotent: if already POSTED, return 200 with current state.

3) POST /api/admin/growth/queue/:id/mark-ready (optional)
Body: { posted: false }
Reverts to READY.

All endpoints require Admin auth.

──────────────────────────────────────────────────────────────────────────────
F) FINISHING REQUIREMENTS
──────────────────────────────────────────────────────────────────────────────
1) Update docs:
- docs/GROWTH_AGENT.md: “TikTok Manual Mode” section including env vars and Admin workflow.

2) Add minimal tests:
- Schema validation test for TikTokPackageSchema
- Dedupe test: running job twice does not create duplicates

3) Do NOT attempt to connect TikTok API.
We are only building “manual mode publishing queue” integration.

DELIVER BACK TO ME
After implementation, summarize:
- Files changed
- New env vars required
- Where in Admin UI I go to find TikTok queue items
- How to mark posted