You are working inside the PackPTS.com codebase (Replit). Implement a clean, production-ready CardHedge client module and three backend endpoints that wrap CardHedge:

1) POST /api/cardhedge/search
   - wraps POST https://api.cardhedger.com/v1/cards/card-search

2) POST /api/cardhedge/search-sorted
   - wraps POST https://api.cardhedger.com/v1/cards/search-cards-wsort

3) POST /api/cardhedge/card-details
   - wraps POST https://api.cardhedger.com/v1/cards/card-details

REQUIREMENTS
- Read CardHedge API key from env var: CARDHEDGE_API_KEY
- Never expose the key to the browser.
- Add strong request validation and helpful errors (422 mapping).
- Normalize image URLs returned by CardHedge:
  - If image begins with "//" prepend "https:"
  - If empty/null, treat as missing
- Enforce “raw_images_only” option support and default it to TRUE for gameplay calls.
- Add placeholder/stock image filtering:
  - If `raw_images_only` is true, discard cards whose image looks like a placeholder/stock/illustration:
    - image contains "placeholder"
    - image contains "example"
    - image contains "appforest_uf/example"
    - or description contains "illustration" (case-insensitive)
  - If the filter removes too many results, automatically paginate to find enough valid cards (see below).
- Add pagination helpers:
  - Provide a server utility that can fetch N valid cards by iterating pages until enough are found or pages exhausted.
- Add server-side caching (in-memory LRU is fine):
  - Cache CardHedge responses for 60 seconds by (endpoint + requestBody hash)
  - Cache card-details by card_id for 5 minutes
- Return consistent response objects to the frontend:
  {
    ok: true,
    pages,
    count,
    cards: [
      {
        card_id,
        description,
        player,
        set,
        number,
        variant,
        category,
        rookie,
        gain,
        gain_30day,
        sales_7day,
        sales_30day,
        image_url,      // normalized absolute URL
        prices: [...]
      }
    ]
  }
- Map CardHedge fields:
  - "7 Day Sales" -> sales_7day
  - "30 Day Sales" -> sales_30day
- Add logging:
  - Log per request: endpoint, page/page_size, count returned, count after filtering, latency
  - Do NOT log the API key
- After implementation, run tests:
  - Add a lightweight server test script that hits:
    a) /api/cardhedge/search (Baseball, search "Shohei Ohtani", page_size 20)
    b) /api/cardhedge/search-sorted (search "Trout rookie", sort_by "sales_30day", page_size 20)
    c) /api/cardhedge/card-details (use a card_id from (a))
  - Ensure tests print results and confirm images are normalized and placeholders filtered.

IMPLEMENTATION DETAILS (USE THESE FILES / STRUCTURE)
- Create:
  /server/integrations/cardhedge/client.ts
  /server/integrations/cardhedge/normalize.ts
  /server/integrations/cardhedge/fetchEnoughCards.ts
  /server/routes/cardhedgeRoutes.ts   (Express router)
  /server/utils/cache.ts             (simple LRU/Map TTL cache)
  /server/tests/cardhedge.smoke.ts   (node script)
- Wire the router into the server (whatever your existing Express entry is, e.g. server/index.ts or server/app.ts).

VALIDATION RULES
- For /search and /search-sorted:
  - Require at least one of: search, set, category, player
  - page default 1; page_size default 20; clamp page_size to [1..100]
  - category allowed values: Baseball, Basketball, Football, Pokemon, Hockey, Soccer (keep extensible)
  - raw_images_only default true
  - rookie is boolean or string "Rookie" (accept either and normalize)
- For /card-details:
  - require card_id (string)
  - raw_images_only default true

PAGINATION “FETCH ENOUGH” BEHAVIOR
- Provide function fetchEnoughCards(params, minCards=20, maxPagesToScan=5):
  - starts at page=params.page||1
  - requests pages sequentially
  - normalizes and filters each page’s cards
  - accumulates until minCards reached or pages exhausted/maxPagesToScan reached
  - returns { pages_scanned, cards: accumulated, count, pages }

IMPORTANT
- Do not change unrelated application logic.
- Keep all CardHedge calling server-side only.
- Make the code resilient to CardHedge returning unexpected shapes (missing fields, null images).

DELIVERABLE
- Commit the files above, wire the routes, and ensure the smoke test runs and passes.

Now implement all of the above.