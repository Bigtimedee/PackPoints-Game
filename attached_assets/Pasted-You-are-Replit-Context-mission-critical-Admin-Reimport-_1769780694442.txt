You are Replit.

Context (mission-critical):
Admin “Reimport Warning” shows: “Imported 0 playable cards (495 fetched, 395 purged)”. That means our importer successfully fetched cards from Card Hedge, but our “playable” filter/validation is incorrectly rejecting 100% of them (or we’re mis-parsing image URLs so they appear invalid). PackPTS becomes unplayable.

Goal:
Fix the importer so a normal Card Hedge fetch produces a meaningful number of playable cards for standard sets (e.g., 1987 Topps Baseball). “Playable” must reflect reality: a card is playable if it has a real, displayable card image (raw or graded) and is not a known placeholder/blank image. We must NOT reject everything.

DO NOT change pricing logic. Focus only on Card Hedge fetching, normalization, image validation, playable classification, and import/purge behavior.

========================================================
1) FIND THE EXACT REASON “PLAYABLE=FALSE” FOR ALL CARDS
Locate the importer code where it:
- fetches Card Hedge results
- normalizes “image”/image_url
- decides playable=true/false
- purges previous cards
- writes import summary text “Imported X playable…”

Add temporary debug logging for one import run:
- log the first 10 fetched card objects (sanitize)
- specifically log:
  - raw card.image from API
  - normalized image_url we store
  - why the card was rejected (explicit reason enum)
  - the final playable boolean

Implement a reject reason enum like:
- MISSING_IMAGE
- BAD_URL
- PLACEHOLDER_DETECTED
- IMAGE_FETCH_FAILED
- OTHER_VALIDATION

========================================================
2) FIX IMAGE URL NORMALIZATION (COMMON ROOT CAUSE)
Card Hedge returns image URLs sometimes as:
- protocol-relative: "//s3.amazonaws.com/..."
- http instead of https
- signed URLs
- paths that need prefixing
If our code treats "//..." as invalid, we’ll reject everything.

Implement normalizeCardImageUrl(url):
- if url starts with "//" => prepend "https:"
- trim whitespace
- if url is empty/null => return null
- if url is non-http(s) => return null
Store the normalized https URL.

Also ensure frontend uses the normalized URL.

========================================================
3) STOP OVER-REJECTING REAL CARD IMAGES
Right now we’re likely rejecting:
- graded slab photos (PSA/BGS slabs)
- “graded.jpg” filenames
- images with certain dimensions
- any image that fails a single fetch attempt

New rule:
A card is playable if:
- image_url is a valid http(s) URL after normalization
- AND it is NOT a known placeholder/blank image
That’s it.

Placeholder detection (robust, not over-broad):
- If image URL hostname/path matches any known placeholder domains or filenames we control (e.g. “example.jpg”, “placeholder”, “blank”, “default-card”, “silhouette”)
- OR the API returns an explicit placeholder asset (if present)
DO NOT reject based on “graded”/“slab”/“PSA” keywords.
DO NOT reject just because it’s not a “raw” photo.

If we previously set raw_images_only=true and that returns few/none, we should:
- Attempt raw_images_only=true first
- If playable_count is below a threshold (e.g. < 50), automatically retry the same query with raw_images_only=false and import those results as playable (while still excluding known placeholders)
This gives us a reliable fallback and prevents “0 playable” scenarios.

========================================================
4) FIX PURGE LOGIC SO IT DOESN’T DELETE GOOD CARDS BEFORE WE CONFIRM NEW ONES
Right now “395 purged” plus “0 playable imported” can wipe the set.

Change import flow to:
- fetch + validate + compute would_import_playable_count FIRST
- if would_import_playable_count == 0:
   - DO NOT purge existing set cards
   - mark the import job as WARNING/FAILURE and keep previous playable cards intact
- if would_import_playable_count > 0:
   - proceed with purge + insert/upsert in a transaction

This alone prevents catastrophic wipeouts.

========================================================
5) IMPLEMENT “IMPORT JOB” SUMMARY THAT IS HONEST + ACTIONABLE
Update the admin toast/message to:
- “Imported {playable_count} playable cards ({fetched_count} fetched, {purged_count} purged)”
If playable_count==0:
- “Reimport failed: 0 playable cards. Kept existing cards. Top reject reasons: …”

Also persist import job details (set_id, fetched_count, playable_count, purged_count, top_reject_reasons).

========================================================
6) REQUIRED TESTS (YOU MUST RUN)
After implementing:
A) Reimport a known set (1987 Topps Baseball)
B) Verify:
- playable_count > 0
- Admin set row shows a non-zero “Cards” count
- Start a game and confirm at least 3 consecutive cards render images (no blank/placeholder)
C) Print test output + a small debug report:
- sample 5 imported cards with their image_url and playable=true
- top reject reasons count

========================================================
DELIVERABLES
- Code changes implementing: URL normalization, improved playable rules, raw_images_only fallback, safe purge gating, and truthful import messaging.
- List files changed.
- Paste console output from the test run and the import job summary.