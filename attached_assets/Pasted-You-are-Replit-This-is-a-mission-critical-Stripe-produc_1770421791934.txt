You are Replit. This is a mission-critical Stripe production configuration fix.

Problem:
PackPTS checkout is still running in Stripe “Sandbox/Test Mode” (screenshot shows “Sandbox” badge and a test checkout experience). Despite prior attempts, the app continues to use test keys. We must force PackPTS to ONLY use Stripe PRODUCTION keys in production deployments, and prevent any path that can create a test-mode Checkout Session when the app is running in production.

Non-negotiables:
- Production environment (packpts.com) MUST use Stripe LIVE keys only.
- Test keys may ONLY be used on non-production hosts/environments (localhost, preview, staging).
- If production keys are missing/invalid, the app must fail CLOSED (block checkout + show a clear admin/dev error), not silently fall back to test keys.
- Do NOT “guess” keys. Read them from environment variables only.
- Implement defensive checks so it is impossible to create a Checkout Session with a test key on the production host.
- After implementation, PRINT/LOG which Stripe mode is active (LIVE vs TEST) on server startup and on each checkout create call (without printing secrets).
- Update docs + deployment config so the correct secrets are set for production.

Deliverables:
1) Code changes to enforce live-mode keys on production host and remove fallback to test keys.
2) Environment variable standardization and validation at boot.
3) Stripe client instantiation centralized on server only.
4) Webhook verification using LIVE webhook secret on production.
5) A smoke test script that calls “create checkout session” locally and confirms it fails if live keys missing and succeeds only when live keys present.

────────────────────────────────────────────────────────────
A) AUDIT THE CURRENT STRIPE IMPLEMENTATION (DO THIS FIRST)
Search the repo for Stripe usage and configuration:
- “stripe”
- “STRIPE”
- “sk_test”
- “sk_live”
- “pk_test”
- “pk_live”
- “CHECKOUT_SESSION”
- “Stripe()”
- “STRIPE_SECRET_KEY”
- “STRIPE_PUBLISHABLE_KEY”
- “process.env”
- “import.meta.env”
- any client-side Stripe initialization

Identify and REMOVE any of these anti-patterns if present:
- defaulting STRIPE_SECRET_KEY to a test key
- selecting key based on NODE_ENV without host validation
- storing secret key in frontend env (VITE_* etc.)
- building checkout sessions on the client
- code paths that attempt “try test if live fails”
- using Stripe CLI test webhook secret in production

────────────────────────────────────────────────────────────
B) STANDARDIZE ENV VARS (SINGLE SOURCE OF TRUTH)
1) Define these environment variables (server-side only):
- STRIPE_SECRET_KEY_LIVE          (required in production)
- STRIPE_WEBHOOK_SECRET_LIVE      (required in production)
- STRIPE_SECRET_KEY_TEST          (allowed for local/staging only)
- STRIPE_WEBHOOK_SECRET_TEST      (allowed for local/staging only)
- STRIPE_PUBLISHABLE_KEY_LIVE     (frontend can read, safe)
- STRIPE_PUBLISHABLE_KEY_TEST     (frontend can read, safe)

2) Production (packpts.com) MUST set:
- STRIPE_SECRET_KEY_LIVE
- STRIPE_WEBHOOK_SECRET_LIVE
- STRIPE_PUBLISHABLE_KEY_LIVE
and MUST NOT set STRIPE_SECRET_KEY_TEST as a fallback.

3) Add a strict boot-time validator:
- Determine “runtime environment” by BOTH:
  a) explicit ENV var APP_ENV = “production” | “staging” | “development”
  b) request host allowlist (packpts.com, www.packpts.com) for production enforcement
- If APP_ENV=production OR host is production, require LIVE secrets or crash server startup with a clear error.

────────────────────────────────────────────────────────────
C) IMPLEMENT STRICT STRIPE MODE SELECTION (SERVER ONLY)
Create a single module: /server/lib/stripe.ts (or equivalent)

Requirements:
1) Never instantiate Stripe in the browser with secret keys.
2) Export:
- getStripeMode({ host }): “live” | “test”
- getStripeServerClient({ host }): Stripe (initialized with correct secret key)
- getStripePublishableKey({ host }): string (live/test publishable key)

Logic:
- if host is in PROD_HOSTS = [“packpts.com”, “www.packpts.com”] => mode = “live”
- else mode = APP_ENV === “production” ? “live” : “test” (but host wins)
- when mode = “live”, require STRIPE_SECRET_KEY_LIVE and STRIPE_WEBHOOK_SECRET_LIVE
- when mode = “test”, allow test vars; if missing, throw

Hard fail:
- If mode is live and STRIPE_SECRET_KEY_LIVE does not start with “sk_live_”, throw.
- If mode is test and STRIPE_SECRET_KEY_TEST does not start with “sk_test_”, throw.
- Do NOT ever fall back from live->test.

Also log on server startup:
- “Stripe mode active: LIVE” or “Stripe mode active: TEST”
- “Stripe key prefix: sk_live_****” (mask everything after prefix)

────────────────────────────────────────────────────────────
D) UPDATE CHECKOUT SESSION CREATION ENDPOINT
Find the endpoint that creates Checkout Sessions (e.g., POST /api/stripe/checkout or /api/bundles/checkout).
Modify it to:
1) Read the request host:
- const host = req.headers.host || new URL(req.url).host
2) Use getStripeServerClient({ host })
3) Use the correct price/product creation flow you already have, but ensure:
- success_url and cancel_url use https://packpts.com/... when host is production
- customer_email is allowed
4) Before calling stripe.checkout.sessions.create, assert mode:
- if getStripeMode(host) !== “live” AND host is prod host => throw (should be impossible)
5) Return session.url

Add structured logging (no secrets):
- checkout_create: { host, mode, bundleSku, usdCents, packpts, userId, stripeKeyType: “live”|“test” }

────────────────────────────────────────────────────────────
E) UPDATE FRONTEND STRIPE LOADING (PUBLISHABLE KEY ONLY)
1) Ensure the frontend uses ONLY publishable keys and selects by host:
- On production host, always use STRIPE_PUBLISHABLE_KEY_LIVE
- Else, use test publishable key

Implementation:
- Add an endpoint GET /api/stripe/config that returns:
  { mode, publishableKey }
- Frontend calls this and initializes Stripe.js with publishableKey.

Do NOT embed both keys in the bundle if you can avoid it.
If you must embed, only embed the live publishable key in production builds.

────────────────────────────────────────────────────────────
F) WEBHOOKS (LIVE VS TEST)
1) Locate webhook handler (e.g., /api/stripe/webhook).
2) Verify signatures using the correct webhook secret selected by host/mode:
- In production host: STRIPE_WEBHOOK_SECRET_LIVE only
- Else: STRIPE_WEBHOOK_SECRET_TEST

3) Hard fail if host is production and webhook secret is not live.

4) Ensure webhook endpoint uses raw body (Stripe requirement). Fix body parsing if needed.

────────────────────────────────────────────────────────────
G) DEPLOYMENT CONFIG (THIS IS LIKELY THE ROOT CAUSE)
Replit commonly has separate “Secrets/Environment variables” per repl or deployment.

Do the following:
1) In Replit “Secrets” for the PRODUCTION deployment (not the dev workspace):
- Set STRIPE_SECRET_KEY_LIVE
- Set STRIPE_WEBHOOK_SECRET_LIVE
- Set STRIPE_PUBLISHABLE_KEY_LIVE
- Set APP_ENV=production
- Remove/Unset STRIPE_SECRET_KEY_TEST and STRIPE_PUBLISHABLE_KEY_TEST if they are being picked up by prod.

2) Add a runtime banner on /admin or server logs:
- “LIVE STRIPE ENABLED” when in live mode
- “TEST STRIPE ENABLED” otherwise

3) Ensure packpts.com points to the production deployment, not a preview or dev instance.

────────────────────────────────────────────────────────────
H) ADD A “FAIL CLOSED” UI MESSAGE
If checkout creation fails because LIVE keys are missing/invalid, show:
- “Payments are temporarily unavailable. Please try again later.”
And log the real error server-side.

Do NOT auto-switch to test.

────────────────────────────────────────────────────────────
I) SMOKE TESTS (REQUIRED)
1) Add a server-side script: scripts/stripe-smoke.ts
- Calls the checkout session creation function with host=packpts.com
- Expects it to throw if STRIPE_SECRET_KEY_LIVE missing
- Expects mode “live” when host=packpts.com
2) Add another test with host=localhost
- Expects mode “test”
3) Run tests and print output.

────────────────────────────────────────────────────────────
J) ACCEPTANCE CRITERIA
- Visiting packpts.com and initiating checkout MUST NOT show “Sandbox”.
- Server logs must confirm “Stripe mode active: LIVE” for packpts.com.
- Any attempt to use test keys on prod host must be blocked with a clear error.
- Webhooks verify using LIVE secret on prod host.

Proceed now: implement all changes, list modified files, and run tests.