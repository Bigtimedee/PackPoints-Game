MISSION-CRITICAL UI FIX (iPhone 17 / Safari): The “Submit Answer” button is missing on the mobile gameplay screen. Fix the UI so a persistent, visible “Submit Answer” button always renders on iOS Safari (including iPhone 17), is reachable above the bottom browser/nav bars, and is not hidden by overflow/viewport or z-index issues.

IMPORTANT INSTRUCTION
After implementing the fix, DO NOT attempt to test it. The user will manually test.

STACK (do not change)
React 18 + Vite + TypeScript + Tailwind + shadcn/ui
Routing: Wouter

LIKELY CAUSE
On iOS Safari, the bottom browser UI + safe area + any fixed bottom nav can overlap or clip content when the page container uses:
- height: 100vh (broken on iOS)
- overflow: hidden on parent
- fixed positioned elements without safe-area padding
- a footer/nav with higher z-index covering the submit area
Result: the Submit button is rendered but off-screen or covered, OR it isn’t rendered due to responsive conditional logic.

GOAL
1) Always render a Submit button in gameplay UI (for all screen sizes).
2) Make it sticky/fixed at bottom with safe-area padding, above any bottom nav.
3) Ensure no parent container clips it (overflow).
4) Ensure it is reachable with scrolling and has correct z-index.

========================================================
A) GAMEPLAY COMPONENT REQUIREMENTS

In the gameplay question view component (e.g., src/pages/Play/QuestionView.tsx or similar):
- Add a “Submit Answer” button that is ALWAYS present.
- Disabled until an answer is selected.
- On click, calls existing submit handler (or placeholder if not implemented yet).

Even if you already have submit logic, DO NOT rely on desktop-only placement. Implement mobile-first sticky action bar.

========================================================
B) IMPLEMENT A STICKY “ACTION BAR” (MOBILE)

Add at the bottom of the gameplay page:

<div className="fixed left-0 right-0 bottom-0 z-50 border-t bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/75">
  <div className="mx-auto max-w-3xl px-4 py-3 pb-[calc(env(safe-area-inset-bottom)+12px)]">
    <Button
      className="w-full h-12 text-base"
      disabled={!selectedChoice || submitting}
      onClick={onSubmit}
    >
      Submit Answer
    </Button>
    {errorMsg && <div className="mt-2 text-sm text-red-400">{errorMsg}</div>}
  </div>
</div>

Key points:
- fixed bottom
- z-50 so it’s above content
- pb uses env(safe-area-inset-bottom) to avoid iOS home indicator/bars
- full-width button

========================================================
C) PREVENT CONTENT FROM BEING COVERED

Because we’re fixing a fixed bottom bar, add padding to the main gameplay content container so the last options are not hidden behind the action bar:

In the gameplay page wrapper (the scrolling container):
- Add: pb-[calc(env(safe-area-inset-bottom)+96px)]

Example:
<div className="min-h-[100dvh] pb-[calc(env(safe-area-inset-bottom)+96px)]">
  ...game content...
</div>

Use 100dvh (dynamic viewport height) NOT 100vh.

========================================================
D) FIX iOS SAFARI VIEWPORT HEIGHT (GLOBAL)

In your global CSS (src/index.css):
- Ensure the app uses dynamic viewport units on mobile Safari:

html, body {
  height: 100%;
}

#root {
  min-height: 100dvh;
}

Also search for any use of h-screen or 100vh in gameplay layouts and replace with min-h-[100dvh] or h-[100dvh].

========================================================
E) REMOVE CLIPPING THAT HIDES THE BUTTON

Search gameplay parent containers for:
- overflow-hidden
- position: relative with children absolute/fixed inside a clipped container
- z-index stacking contexts that might hide the fixed bar

If any parent container wraps the gameplay page with overflow-hidden, change it to overflow-visible or move the fixed action bar OUTSIDE that container (render it at the page root level).

========================================================
F) ENSURE THE BUTTON IS NOT CONDITIONALLY REMOVED ON MOBILE

Search for any conditional rendering like:
- {isDesktop && <SubmitButton/>}
- hidden md:block patterns
Remove these. Submit must render for all breakpoints.

========================================================
G) BOTTOM NAV COMPATIBILITY

If you have a persistent bottom navigation (Play/Ranks/Store/Redeem/Profile), ensure:
- The bottom nav does NOT overlap the Submit bar during gameplay.
Implement a gameplay-only rule:
- Hide bottom nav during active question screen, OR
- Move bottom nav above the submit bar, OR
- Increase bottom padding so both are visible.

Recommended:
- Hide bottom nav only on the gameplay question route:
  if (route starts with /play/match or /play/question) hide bottom nav.

========================================================
H) DELIVERABLE

- A fixed bottom “Submit Answer” bar with safe-area padding
- Gameplay content padded to avoid being covered
- Replace 100vh/h-screen with 100dvh where relevant
- Remove any overflow/z-index issues that caused the button to disappear
- Do NOT run tests; user will manually test