Fix Marketplace redemption gating so ALL UI components (header points, progress pill “X/5,000”, and redeem buttons) use the SAME numeric field: wallet.availablePts from GET /api/wallet/balance.

PROBLEM
Header shows 11,552 but progress shows 0/5,000 and buttons disable as “Not Enough Points”. This indicates the Marketplace page is using a different value (undefined defaulting to 0, wrong field name, wrong bucket, or string parsing).

TASKS
1) BACKEND: Guarantee /api/wallet/balance returns numeric fields:
{
  availablePts: number,
  pendingPts: number,
  lockedPts: number,
  debtPts: number,
  status: 'NORMAL'|'RESTRICTED'|'FROZEN',
  totalPts: number   // optional, computed
}
Ensure availablePts is a number (no commas, no strings).

2) FRONTEND: Create a single source-of-truth hook:
- src/hooks/useWalletBalance.ts
  - fetch GET /api/wallet/balance
  - Zod-validate response shape and types
  - return typed object

3) FRONTEND: Replace ALL instances of “points” / “balance” on /marketplace:
- Header points badge uses wallet.availablePts (or show “Available” explicitly).
- Progress pill uses wallet.availablePts:
  progressText = `${fmt.format(wallet.availablePts)}/${fmt.format(requiredPts)}`
- Button enable uses:
  canRedeem = wallet.status==='NORMAL' && wallet.debtPts===0 && wallet.availablePts >= costPts

4) REMOVE ANY string parsing:
- No parseInt / Number on formatted strings anywhere.
- Only format for display at render:
  fmt.format(wallet.availablePts)

5) ADD A DEV ASSERTION:
If wallet.availablePts is undefined or NaN, throw in dev:
if (!Number.isFinite(wallet.availablePts)) console.error("wallet.availablePts invalid", wallet)

6) ADD A TEST:
Marketplace gating test:
- availablePts=11552 => 5,000 tier enabled, 10,000 tier enabled
- availablePts=0 => disabled
- status='RESTRICTED' or debtPts>0 => disabled

DELIVERABLE
- Consolidated wallet usage on Marketplace to wallet.availablePts
- All UI elements consistent (badge, progress, and buttons)
- Type validation + test preventing regression